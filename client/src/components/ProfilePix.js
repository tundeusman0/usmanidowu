import React from 'react';
import profile from '../images/profile.jpg';

const ProfilePix = () => {
  return (
    <div className="portPix">
      <img src={profile} alt="profile pics" />
    </div>
  );
};

export default ProfilePix;
